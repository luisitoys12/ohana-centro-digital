# Despliegue en Netlify — este workflow ya no es necesario.
# Netlify detecta el push a main y despliega automáticamente.
# Se conserva el archivo por referencia histórica.
#
# Para reactivar GitHub Pages en el futuro, descomentar el contenido.
